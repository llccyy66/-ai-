# 异构任务调度系统软件开发需求文档（AI Agent执行版）

生效日期：文档生成当日 \| 版本号：V1\.0 \| 核心要求：无歧义 100% 严格执行，禁止修改核心规则

---

## 1\. 术语定义（执行前必须 100% 明确，无任何歧义解释空间）

|术语名称|唯一明确含义|
|---|---|
|A 类任务|带文生图环节的长耗时任务，完整执行链路为【TTS 语音生成→文生图提示词生成→ASR 文本强制对齐→文生图正式渲染】，核心特征为文生图环节为长耗时 IO 阻塞型操作|
|B 类任务|无文生图环节的短耗时任务，执行链路为【TTS 语音生成→ASR 文本强制对齐】，核心特征为全链路均为短耗时操作，无长耗时环节|
|一级大队列|系统顶层的 2 个完全隔离的任务队列，分别为【A 类任务专属队列】、【B 类任务专属队列】|
|二级子队列|嵌入 B 类任务调度逻辑的环节级执行队列，共 2 个，分别为【TTS 专属子队列】、【ASR 专属子队列】|
|强依赖规则|系统不可突破的执行顺序底线，共 2 条：<br>① 同一任务的 TTS 语音生成必须 100% 执行完成后，该任务的 ASR 文本强制对齐才可执行<br>② 同一 A 类任务的文生图提示词生成必须 100% 执行完成后，该任务的文生图正式渲染才可执行|
|文生图执行阶段|唯一判定标准为【A 类任务的文生图 API 请求已成功发起、正式进入等待 API 响应的 IO 阻塞状态】，不包含文生图提示词生成等任何前置环节|

---

## 2\. 需求背景

当前系统存在两类耗时特征差异极大的异构任务，需开发一套专属调度系统，实现以下核心目标：

1. 以 A 类长耗时任务为核心锚点，优先保障 A 类任务全链路执行顺畅

2. 100% 利用 A 类任务文生图阶段的 IO 阻塞窗口，填充 B 类短耗时任务执行，杜绝计算 / API 资源闲置

3. 避免长耗时任务阻塞短耗时任务，避免两类任务的资源竞争

4. 全程严格遵守强依赖规则，无任何执行顺序错误

5. 最大化压榨 TTS、ASR 环节的资源利用率，保证对应队列无空闲

---

## 3\. 核心设计理念（不可修改的底层逻辑）

1. 锚点调度：以 A 类长耗时任务为调度时间锚点，所有 B 类任务的调度均围绕 A 类任务的执行状态展开

2. 两级隔离：采用两级队列架构，顶层实现长短任务的完全隔离调度，底层实现环节级精细化管控

3. 资源拉满：核心目标为全链路无资源闲置，A 类任务文生图执行期间，只要有可执行的 B 类任务，TTS、ASR 子队列永远不空闲

4. 优先级保障：A 类任务全链路优先级永远高于 B 类任务，绝对禁止 B 类任务阻塞 A 类任务执行

---

## 4\. 系统架构设计（不可修改的核心架构）

### 4\.1 顶层一级大队列架构

- 队列 1：A 类任务专属队列，仅用于存放所有待执行的 A 类任务，仅调度 A 类任务执行，与 B 类队列完全隔离

- 队列 2：B 类任务专属队列，仅用于存放所有待执行的 B 类任务，仅调度 B 类任务执行，与 A 类队列完全隔离

### 4\.2 底层二级子队列架构

- 子队列 1：TTS 专属子队列，仅用于执行所有任务（含 A 类、B 类）的 TTS 语音生成环节

- 子队列 2：ASR 专属子队列，仅用于执行所有任务（含 A 类、B 类）的 ASR 文本强制对齐环节

- 硬性要求：二级子队列全程严格遵守强依赖规则，所有入队任务必须满足对应前置条件

---

## 5\. 系统强制执行规则（100% 严格执行，无任何例外）

### 5\.1 启动优先级执行规则

- 触发条件：系统启动、接收到批量任务并完成一级队列分类后

- 执行动作：系统优先检测 A 类任务专属队列，若队列内存在待执行的 A 类任务，首个启动执行的任务必须为 A 类任务；若 A 类任务专属队列为空，系统直接正常调度执行 B 类任务专属队列内的待执行任务，无任何阻塞等待

### 5\.2 B 类任务触发执行规则

- 触发条件：系统实时监测到 A 类任务正式进入文生图执行阶段（以术语定义中的唯一标准判定）

- 执行动作：立即自动触发 B 类任务专属队列的调度逻辑，开始向二级子队列推入符合条件的 B 类任务

### 5\.3 B 类任务精细化调度规则

- 核心目标：保证 TTS 专属子队列、ASR 专属子队列永远不出现空闲状态，最大化压榨短耗时环节的资源利用率

- 执行动作：系统实时监测 2 个二级子队列的运行状态，只要任意子队列出现空闲资源，立即从 B 类任务专属队列中，推入满足对应子队列前置依赖条件的待执行 B 类任务，无需等待前一个 B 类任务全链路执行完成

- 禁止动作：不得将 B 类任务设置为完全串行执行，仅可通过子队列空闲状态动态推入

### 5\.4 A 类队列保活执行规则

- 触发条件：系统实时监测到当前正在执行的 A 类任务全链路 100% 执行完成

- 执行动作：立即从 A 类任务专属队列中，推入下一个待执行的 A 类任务，启动该任务的全链路执行，直到 A 类任务专属队列内无待执行任务为止

### 5\.5 B 类队列续填执行规则

- 触发条件：当前作为调度锚点的 A 类任务全链路执行完成

- 执行动作：系统立即检测同批次触发的 B 类任务的执行状态，若该批次 B 类任务已全部执行完成，立即从 B 类任务专属队列中推入新的待执行 B 类任务，持续填充后续 A 类任务的文生图执行窗口

### 5\.6 强依赖不可突破规则

- 执行动作 1：ASR 专属子队列必须设置入队强制前置校验，只有对应任务的 TTS 语音生成环节 100% 执行完成，该任务的 ASR 环节才可进入 ASR 专属子队列的待调度池，否则绝对不允许入队执行

- 执行动作 2：A 类任务的文生图环节必须设置入队强制前置校验，只有对应任务的文生图提示词生成环节 100% 执行完成，该任务的文生图环节才可启动执行，否则绝对不允许执行

### 5\.7 资源优先级保障规则

- 执行动作：给所有任务环节划分三级不可突破的优先级，二级子队列调度时，严格按照优先级从高到低分配资源，高优先级任务未执行完成前，不得给低优先级任务分配资源

    - P0 最高优先级：A 类任务的所有前置环节（TTS 语音生成、文生图提示词生成、ASR 文本强制对齐）

    - P1 次高优先级：A 类任务的文生图正式渲染环节

    - P2 最低优先级：B 类任务的所有环节（TTS 语音生成、ASR 文本强制对齐）

- 核心目标：绝对保证 A 类任务的前置环节不会被 B 类任务阻塞，不会延迟 A 类任务进入文生图执行阶段的时间

---

## 6\. 系统优化执行规则（100% 严格执行，无任何例外）

### 6\.1 动态水位填充优化规则

- 执行动作：系统实时计算 TTS 专属子队列、ASR 专属子队列的空闲水位（空闲水位 = 子队列最大可承载并发数 \- 当前正在执行的任务数），只要 B 类任务专属队列内有待执行任务、且子队列存在空闲水位，一次性推入与空闲水位数量一致的、满足前置依赖条件的 B 类任务，直接填满子队列空闲资源，减少调度开销

### 6\.2 预调度机制优化规则

- 执行动作：系统基于历史 A 类任务文生图环节的耗时数据，提前预判当前正在执行的 A 类任务的结束时间，提前 10 秒将下一个待执行 A 类任务的前置环节（TTS 语音生成、文生图提示词生成）放入 P0 优先级队列执行，保证当前 A 类任务执行完成的瞬间，下一个 A 类任务已完成所有前置环节，可直接进入文生图执行阶段，实现 A 类任务链路零间隙衔接

### 6\.3 任务状态追踪优化规则

- 执行动作：给每一个任务设置标准化全生命周期状态标签，分别为【待调度、执行中、已完成、执行失败】，只有状态为【待调度】且满足对应环节前置依赖条件的任务，才可进入对应队列 / 子队列执行；任务环节执行完成后，立即更新对应状态标签

### 6\.4 监控埋点自优化规则

- 执行动作：给所有任务环节增加耗时、执行成功率、队列空闲率监控埋点，系统基于监控数据自动调整调度参数：如 B 类任务平均耗时远小于 A 类任务文生图耗时，自动提升 B 类任务的填充并发数，无需人工干预即可实现调度策略自优化

---

## 7\. 异常处理机制（100% 严格执行，无任何例外）

### 7\.1 任务执行失败异常处理

- 触发条件：任意任务的任意环节执行失败

- 执行动作：给该任务启动指数退避重试机制，最大重试次数为 3 次；超过最大重试次数仍执行失败的任务，立即移入【执行失败队列】，不再占用正常调度资源，不阻塞后续任务的调度执行

### 7\.2 空队列兜底处理

- 触发条件 1：B 类任务专属队列为空，二级子队列出现空闲

- 执行动作 1：立即停止子队列的无效轮询，进入等待状态，直到 B 类任务专属队列有新的待执行任务入队，再恢复调度

- 触发条件 2：A 类任务专属队列、B 类任务专属队列均为空

- 执行动作 2：系统进入待机状态，停止所有调度轮询，直到有新任务入队，再恢复对应调度逻辑

### 7\.3 极端场景兜底处理

- 场景 1：A 类任务文生图执行已完成，但同批次触发的 B 类任务仍未执行完成，此时新的 A 类任务已启动前置环节

- 执行动作 1：严格按照三级优先级规则，优先保障新 A 类任务 P0 级前置环节的资源分配，剩余空闲资源再分配给未完成的 B 类任务，绝对不允许 B 类任务阻塞 A 类任务执行

- 场景 2：B 类任务专属队列已清空，但 A 类任务仍处于文生图执行阶段，二级子队列出现空闲

- 执行动作 2：按照空队列兜底规则执行，停止无效轮询，等待新的 B 类任务入队，无任何无效操作

---

## 8\. 验收标准（执行完成后必须 100% 全部满足，否则视为执行失败）

1. 强依赖合规性：100% 的任务执行严格遵守强依赖规则，无任何 ASR 先于 TTS 执行、文生图先于提示词生成执行的情况

2. 优先级合规性：A 类任务的 P0 级前置环节永远优先于 B 类任务执行，无任何 A 类任务前置环节被 B 类任务阻塞的情况

3. 资源利用率合规性：A 类任务处于文生图执行阶段时，只要 B 类任务专属队列有待执行任务，二级子队列（TTS、ASR）无任何空闲状态，资源利用率 100%

4. A 类队列保活合规性：只要 A 类任务专属队列有待执行任务，当前一个 A 类任务执行完成后，下一个 A 类任务立即启动执行，无任何间隙等待

5. 异常处理合规性：任务执行失败后按规则重试，超过重试次数移入失败队列，无单个失败任务阻塞整个调度流程的情况

6. 启动规则合规性：系统启动后，只要 A 类队列有待执行任务，首个执行的任务必须为 A 类任务，无例外

---

## 9\. 绝对禁止执行项（任何情况下都不允许添加 / 修改，否则视为执行失败）

1. 绝对禁止添加任何针对 A 类任务文生图环节的限流保护、并发数上限限制相关的逻辑

2. 绝对禁止修改、突破系统的两级队列核心架构

3. 绝对禁止修改、突破强依赖规则、三级优先级规则

4. 绝对禁止将 B 类任务设置为完全串行执行，必须按照子队列空闲状态动态调度

5. 绝对禁止修改文生图执行阶段的唯一判定标准

6. 绝对禁止修改、删减本需求文档中任何强制执行规则，仅可在不违背核心规则的前提下补充兼容逻辑

> （注：文档部分内容可能由 AI 生成）
